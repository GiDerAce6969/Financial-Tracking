# FinTrack - A Personal Finance & Investment Dashboard

![Screenshot of FinTrack Dashboard](URL_TO_YOUR_DASHBOARD_SCREENSHOT)

A clean, single-page web application to track income, expenses, and investments. This project is built entirely with frontend technologies and uses browser `localStorage` to save data, requiring no backend or database.

---

## ✨ Key Features

-   **📝 Add Entries:** Log income, expenses, and investments through a single, dynamic form.
-   **💾 Data Persistence:** All data is saved in your browser's `localStorage`—it's fast, private, and works offline.
-   **📊 At-a-Glance Summary:** Instantly view your total income, expenses, net cash flow, and total investment value.
-   **📈 Interactive Charts:** Visualize your expense categories and investment allocation with dynamic doughnut charts.
-   **📜 Transaction History:** See a clear, sorted list of all your cash and investment entries.
-   **🗑️ Simple Deletion:** Easily remove any entry with a single click.

---

## 🛠️ Tech Stack

-   **HTML5**
-   **CSS3** (with modern grid/flexbox layouts)
-   **Vanilla JavaScript** (for all application logic)
-   **Chart.js** (for data visualization)
-   **Browser `localStorage`** (for client-side data storage)

---

## 🚀 How to Use

This is a frontend-only project. No complex setup is required!

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/your-username/fintrack-personal-finance-dashboard.git
    ```
2.  **Navigate to the directory:**
    ```bash
    cd fintrack-personal-finance-dashboard
    ```
3.  **Open `index.html`:**
    Simply open the `index.html` file in your favorite web browser (like Chrome, Firefox, or Edge).

That's it! The application is ready to use.

---

## 🔮 Future Improvements

-   [ ] **Edit Functionality:** Allow users to edit existing entries.
-   [ ] **Category Management:** A dedicated system for managing expense categories.
-   [ ] **Live Investment Value:** Integrate a free market data API to track the current value of investments.
-   [ ] **Data Export:** Add a feature to export data as a CSV file.
-   [ ] **Filtering & Reporting:** Allow users to view summaries by date range (e.g., this month, last year).
